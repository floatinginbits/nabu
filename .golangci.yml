version: "2"

linters:
  default: standard
  exclusions:
    paths:
      # npm packages can ship stray Go files (e.g. flatted)
      - web/node_modules

formatters:
  enable:
    - gofmt
    - goimports
  exclusions:
    paths:
      - web/node_modules
